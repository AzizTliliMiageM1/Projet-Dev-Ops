Docker Compose — MySQL + application

Ajout d'un fichier `docker-compose.yml` démarrant un service MySQL et
le service application buildé à partir du Dockerfile.
Le volume `db_data` est utilisé pour persister les données.
